[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "adk-mr-movie"
version = "0.1.0"
dependencies = [
  "pydantic>=2,<3",
  "ulid-py>=1.1.0",
  "python-dotenv>=1.0.0",
  "httpx>=0.27.0",
]

[tool.setuptools]
packages = ["movie_night_agent"]
